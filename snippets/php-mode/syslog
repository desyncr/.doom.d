# -*- mode: snippet -*-
# name: syslog
# uuid:
# key: sys
# group: application
# condition: t
# --
if (openlog('${1:DEBUG}', LOG_PID, LOG_LOCAL3)) {
    $data = [
        '$2' => $0
    ];
    syslog(LOG_NOTICE, json_encode($data));

    closelog();
}
